---
layout: default
---
	
<article class=hentry>

	<div id="roll">
		<div title="回到顶部" id="roll_top"></div>
		<div title="" id="ct"></div>
		<div title="转到底部" id="fall"></div>
	</div>	

<header>
{% if page.link %}
<h1><a class=entry-title href="{{ page.link | escape }}">{{ page.title }}</a>&nbsp;→</h1>
{% else %}
<h1 class=entry-title>{{ page.title }}</h1>
{% endif %}

<p class="byline author vcard instapaper_ignore entry-unrelated">
  {% unless page.link %}
  By <a href="{{ site.baseurl }}" rel=author class=fn lang=hr>Chyun</a>
  {% endunless %}
  {% if page.updated %}
  on <time class=published datetime="{{ post.date | date_to_xmlschema }}" pubdate>{{ post.date | date_to_string }}</time>
  <time class=updated datetime="{{ page.updated }}"></time>
  {% else %}
  on <time class=updated datetime="{{ post.date | date_to_xmlschema }}" pubdate>{{ post.date | date_to_string }}</time>
  {% endif %}
</p>
</header>

<div id="post" class="entry-content{% if page.link %} link{% endif %}">
  {{ content }}
</div>

</article>

<aside id="related">
  {% unless page.link %}
  <h2>Also on this site</h2>
  
  <ol class="posts">
    {% for related in site.posts %}
      {% if related.url != page.url and related.link == null and (void == null or void != '2') %}
      <li>
        <span class="date">{{ related.date | date_to_string }}</span>
        ▸ <a href="{{ site.baseurl }}{{ related.url }}">{{ related.title }}</a>
      </li>
      {% capture void %}{% increment related_shown %}{% endcapture %}
      {% endif %}
    {% endfor %}
  </ol>
  {% endunless %}
  
  
  <nav><a class="home" href="{{ site.baseurl }}/blog/">← all posts</a></nav>
</aside>
<div id="disqus_thread"></div>
<script type="text/javascript">
		var disqus_shortname = 'snail1314'; // 注意，这里的 example 要替换为你自己的短域名
		/* * * 下面这些不需要改动 * * */
		(function() {
		  var dsq = document.createElement('script'); dsq.type = 'text/javascript'; dsq.async = true;
    dsq.src = 'http://' + disqus_shortname + '.disqus.com/embed.js';
  (document.getElementsByTagName('head')[0] || document.getElementsByTagName('body')[0]).appendChild(dsq);
})();
</script>
<noscript>Please enable JavaScript to view the <a href="http://disqus.com/?ref_noscript">comments powered by Disqus.</a></noscript>
<a href="http://disqus.com" class="dsq-brlink">blog comments powered by <span class="logo-disqus">Disqus</span></a>
